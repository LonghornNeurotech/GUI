---
phase: 04-transfer-function-and-1d-motor-imagery-tasks
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - TASK-01
  - TASK-02
  - TASK-04
  - TASK-05
  - TASK-06
  - TASK-07

must_haves:
  truths:
    - "1D Left/Right task opens with crosshair, arrow cues, blink prompt in REST only"
    - "1D Up/Down task opens with crosshair, UP/DOWN arrows, identical timing"
    - "MINDFULNESS state emits markers visible in console/XDF"
    - "All state transitions produce LSL markers"
    - "60s mindfulness followed by repeating cue/rest cycles"
  artifacts: []
  key_links:
    - from: "GUI.py"
      to: "tasks/motor_imagery/app.js"
      via: "QUrl mode param + QWebChannel bridge"
      pattern: "mode.*LR|mode.*UD"
---

<objective>
Integration verification -- visually confirm the complete 1D motor imagery task flow for both LR and UD modes.

Purpose: Validates that all Phase 4 components work together end-to-end: transfer function math, JS state machine extensions, GUI wiring, LSL markers, and visual presentation. This is the final gate before marking Phase 4 complete.

Output: Verified integration of all Phase 4 components
</objective>

<execution_context>
@/Users/donmega/Desktop/GUI/.claude/get-shit-done/workflows/execute-plan.md
@/Users/donmega/Desktop/GUI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-transfer-function-and-1d-motor-imagery-tasks/04-01-PLAN.md
@.planning/phases/04-transfer-function-and-1d-motor-imagery-tasks/04-02-PLAN.md
@.planning/phases/04-transfer-function-and-1d-motor-imagery-tasks/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify transfer function integration</name>
  <files></files>
  <action>
    Run the complete test suite to confirm no regressions across all phases:
    ```bash
    python3 -m pytest tests/ -v
    ```

    Then verify the transfer function module is importable and produces correct outputs:
    ```python
    from dsp.transfer_function import apply_transfer_function

    # Dead zone
    assert apply_transfer_function(0.0) == 0.0
    assert apply_transfer_function(0.05) == 0.0

    # Quadratic
    val = apply_transfer_function(0.5, r=1.0)
    assert 0.17 < val < 0.18  # ~0.175

    # Saturation
    assert apply_transfer_function(1.5) == 0.9009

    # R factor effect
    assert apply_transfer_function(0.5, r=3.0) > apply_transfer_function(0.5, r=1.0)
    ```

    Verify GUI.py can import without errors:
    ```bash
    python3 -c "import GUI" 2>&1 | head -5
    ```
    (This may produce Qt warnings about display, which is fine. ImportError would be a problem.)
  </action>
  <verify>
    <automated>cd /Users/donmega/Desktop/GUI && python3 -m pytest tests/ -v && python3 -c "
from dsp.transfer_function import apply_transfer_function
assert apply_transfer_function(0.0) == 0.0
assert apply_transfer_function(0.05) == 0.0
assert abs(apply_transfer_function(0.5) - 0.175000225) < 1e-6
assert apply_transfer_function(1.5) == 0.9009
assert apply_transfer_function(0.5, r=3.0) > apply_transfer_function(0.5, r=1.0)
print('Transfer function integration verified')
"</automated>
  </verify>
  <done>Full test suite passes with no regressions. Transfer function produces correct outputs for all three regions. GUI.py imports without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of 1D LR and 1D UD task flows</name>
  <action>
    Human verifies the complete task system visually by launching the GUI and running both 1D Left/Right and 1D Up/Down tasks with 1 cycle each.

    What was built:
    - Transfer function (dead zone, quadratic, saturation) in dsp/transfer_function.py
    - R factor spinbox in Control Signals panel (default 1.0)
    - MINDFULNESS state with LSL markers (start + end)
    - Arrow cues for LEFT/RIGHT/UP/DOWN
    - White crosshair always visible during task
    - Mode-parameterized task launch (LR/UD via QUrl query string)
    - Two task menu items: "1D Left/Right" and "1D Up/Down"

    Steps to verify:
    1. Launch the GUI: `python3 GUI.py`
    2. Verify R factor spinbox appears in the Control Signals panel (left sidebar) with value 1.00
    3. Click the task launcher dropdown -- verify "1D Left/Right" and "1D Up/Down" appear (old "Motor Imagery Task" should be gone)

    Test 1D Left/Right:
    4. Click "1D Left/Right" -- task window opens
    5. Enter a subject ID and save location, set cycles to 1
    6. Click "Initialize Link" -- instruction screen shows "LEFT / RIGHT" in steps 3 and 4
    7. Click "Enter Mindfulness Phase" -- verify 60s countdown with ocean waves audio
    8. Check console output for marker: {"stop":"None","start":"MINDFULNESS"}
    9. After mindfulness ends, verify marker: {"stop":"MINDFULNESS","start":"REST"}
    10. During REST: white crosshair visible, "BLINK NOW / CLEAR MIND" text, countdown circle
    11. During cue state: large arrow pointing left or right, colored (cyan for LEFT, purple for RIGHT)
    12. Verify no blink text during cue states
    13. After all cycles complete, session ends with "Sync Complete" screen

    Test 1D Up/Down:
    14. Close task window, click "1D Up/Down"
    15. Enter subject ID, set cycles to 1
    16. Click "Initialize Link" -- instruction screen shows "UP / DOWN" in steps 3 and 4
    17. After mindfulness, verify UP/DOWN arrows appear during cue states
    18. Verify markers show UP and DOWN (not LEFT/RIGHT)
  </action>
  <verify>Human visual confirmation of both task modes</verify>
  <done>Human approves both LR and UD task modes work correctly -- crosshair, arrows, markers, timing all verified.</done>
</task>

</tasks>

<verification>
- Full test suite: `python3 -m pytest tests/ -v`
- Transfer function import: `python3 -c "from dsp.transfer_function import apply_transfer_function; print('OK')"`
- Visual verification of both LR and UD task modes
- Console markers confirm MINDFULNESS state transitions
</verification>

<success_criteria>
- All automated tests pass (no regressions)
- 1D Left/Right task shows arrow cues, white crosshair, MINDFULNESS markers, blink prompt REST-only
- 1D Up/Down task shows UP/DOWN arrows with identical timing structure
- R factor spinbox visible and functional
- Human approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-transfer-function-and-1d-motor-imagery-tasks/04-04-SUMMARY.md`
</output>
