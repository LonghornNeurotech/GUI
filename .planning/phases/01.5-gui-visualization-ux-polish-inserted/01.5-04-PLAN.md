---
plan: 01.5-04
title: Verify prior requirements (FILT-07, FILT-08, UX-05)
wave: 2
depends_on:
  - 01.5-01
  - 01.5-02
files_modified: []
requirements:
  - UX-05
  - FILT-07
  - FILT-08
autonomous: true
---

# Plan 01.5-04: Verify Prior Requirements Still Intact

## Goal

Confirm that UX-05, FILT-07, and FILT-08 (completed in prior phase work) are still intact after the changes in this phase. Verification-only -- no code changes expected.

## Tasks

<task id="01.5-04-T1" title="Verify FILT-07: dual-stream XDF recording">
<read_first>
- GUI.py (search for "Filtered_EEG" or "filtered" stream creation in XDF recording code)
</read_first>
<action>
This is a verification-only task. Confirm that:

1. GUI.py creates a second LSL outlet or XDF stream with type "Filtered_EEG" for filtered data.
2. Both raw and filtered streams are pushed during recording.

No code changes expected. If the implementation is missing or broken, flag it as a blocker and do not attempt to fix (it was marked complete in prior phase work).

Run: `grep -n "Filtered_EEG" GUI.py` and verify at least 2 matches (stream creation + push).
</action>
<acceptance_criteria>
- `grep -c "Filtered_EEG" GUI.py` returns >= 1
- FILT-07 confirmed present -- no code change needed
</acceptance_criteria>
</task>

<task id="01.5-04-T2" title="Verify FILT-08: XDF load prefers filtered stream">
<read_first>
- GUI.py (search for "_load_xdf" or "Filtered_EEG" in file loading code)
</read_first>
<action>
This is a verification-only task. Confirm that:

1. The XDF loading code (`_load_xdf` or equivalent) checks for a "Filtered_EEG" stream type.
2. It prefers the filtered stream over raw EEG when both are available.

Run: `grep -n "Filtered_EEG\|prefer.*filtered\|filtered.*prefer" GUI.py`

No code changes expected.
</action>
<acceptance_criteria>
- `grep -c "Filtered_EEG" GUI.py` returns >= 2 (at least one in recording, one in loading)
- FILT-08 confirmed present -- no code change needed
</acceptance_criteria>
</task>

<task id="01.5-04-T3" title="Verify UX-05: all channels selected by default in file mode">
<read_first>
- GUI.py (search for "selectAll\|setCheckState.*Checked\|select_all" in channel loading code)
</read_first>
<action>
This is a verification-only task. Confirm that when a file is loaded, all channels are selected by default (matching streaming mode behavior).

Run: `grep -n "selectAll\|select_all\|setCheckState.*Checked" GUI.py | head -20`

No code changes expected.
</action>
<acceptance_criteria>
- GUI.py contains logic to select all channels when loading a file
- UX-05 confirmed present -- no code change needed
</acceptance_criteria>
</task>

## Verification

```bash
# Prior requirements still intact
grep "Filtered_EEG" GUI.py              # >= 2 matches
grep -n "selectAll\|select_all" GUI.py  # channel selection present
```

## must_haves

- XDF recordings contain both raw and filtered EEG streams (FILT-07 still works)
- Loading an XDF file with both streams prefers the filtered stream (FILT-08 still works)
- Opening a file selects all channels by default so user sees data immediately (UX-05 still works)
